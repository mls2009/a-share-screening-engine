# Sequoia 分组选股 Implementation Plan

**Goal:** 接入 Sequoia-X 七个策略，按策略分组展示依据并标注 K 线，结果可批量保存至自选分组。

**Architecture:** 独立 Python 模块、独立运行记录表、独立 React 页面；仅增加 API 注册、导航和自选来源分组标题兼容。复用现有 ChartPage 的 ConditionMark，不改变其他选股/回测算法。

**Tech Stack:** FastAPI / DuckDB / pandas / React / ECharts。

- [x] 纯策略测试：阈值、窗口、未知值、横截面 RPS、定增日期；实现七策略和逐条依据。
- [x] 服务/API：本地日线快照、异步运行、历史、进度、按策略自选分组、来源及标注持久化；验证不污染原记录。
- [x] 独立深色页面：策略参数、范围、JSON、分组结果、详情/标注、自选目标；验证交互。
- [x] 后端相关回归 147 项、前端 112 项、浏览器回归 5 项及生产构建通过；规则来源与适配差异已记录。
- [x] 在恢复副本上验证全部七组实际行情筛选：5,558 只范围，224 只去重命中，日线截至 2026-09-08，约 5.2 秒。
- [x] 正式启用 8888：原服务未运行，原库 WAL 校验损坏。原库和完整日志已备份，恢复副本保留两只自选及来源；用户已确认后恢复，原件保留；正式页面七个策略入口及 API 已验证。损坏点后的日志不能保证恢复。行情补更仍受 BaoStock 网络错误阻塞，调度会重试。

数据约束：计算截至选择日期的最近市场日；停更股票不视为当天命中；RPS 母体始终为全市场可计算股票；定增只提供当日查询，发行日期不冒充公告日期。现有未提交工作保持原样。
