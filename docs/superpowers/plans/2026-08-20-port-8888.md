# Port 8888 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make port 8888 the documented and verified AStock integrated-service port.

**Architecture:** Keep application code and the Vite development port unchanged. Replace only the integrated Uvicorn commands and browser URL in README, then start the existing app factory on port 8888 and query its catalog endpoint.

**Tech Stack:** Markdown, Uvicorn, FastAPI, curl

---

### Task 1: Switch and verify the integrated-service port

**Files:**
- Modify: `README.md:104-111`

- [ ] **Step 1: Verify the old documented port exists**

Run: `rg -n "8000" README.md`

Expected: three matches covering two Uvicorn commands and the browser URL.

- [ ] **Step 2: Replace the documented port**

Change both `--port 8000` arguments to `--port 8888`, and change the browser URL to
`http://127.0.0.1:8888`.

- [ ] **Step 3: Verify documentation consistency**

Run: `rg -n "8000|8888" README.md`

Expected: no `8000` matches and three `8888` references.

- [ ] **Step 4: Verify the running service**

Run:

```bash
.venv/bin/uvicorn astock.api.app:create_default_app --factory \
  --host 127.0.0.1 --port 8888
curl --fail http://127.0.0.1:8888/api/catalog
```

Expected: Uvicorn listens on `127.0.0.1:8888`, and the catalog endpoint returns JSON.

- [ ] **Step 5: Commit**

```bash
git add README.md docs/superpowers/plans/2026-08-20-port-8888.md
git commit -m "docs: switch service port to 8888"
```

