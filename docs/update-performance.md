# 行情更新提速验证（2026-09-15）

## 改动

- 形态识别新增日期下界，仅遍历需要更新的蜡烛；完整历史仍可用于吞没、三根 K 线和 20 日整理突破等条件的上下文。
- 支撑阻力的极值检测使用滚动窗口，聚类先读取价格数组，避免循环中反复创建 pandas 行对象。聚类顺序、均值和阈值规则保持一致。
- 腾讯历史行情按请求区间的自然日数量限制返回条数，覆盖所有可能的交易日；跨年仍按年取数。约 35 天的校验窗口不再请求 640 条。
- 备用更新队列优先处理已完成的普通股票请求，慢请求不阻塞后面的结果；自选股优先，网络并发仍限制在 4，数据库仍单线程写入。

## 对照结果

在冻结的 4 只真实股票样本（001309.SZ、600000.SH、000001.SZ、688001.SH）上，包含完整技术指标计算、最新日形态识别和支撑阻力检测：

| 环节样本总耗时 | 优化前 | 优化后 |
|---|---:|---:|
| 4 只股票计算 | 1.643 秒 | 0.102 秒 |

计算样本约快 16 倍。技术指标 DataFrame、形态事件和支撑阻力对象逐项完全一致。该数字不包括网络和数据库耗时，不代表全市场任务固定快 16 倍。

真实腾讯接口同窗对照（2026-08-11 至 2026-09-15）：两只股票各返回相同的 26 根 K 线；响应从 56,210 / 53,422 字节降到 4,817 / 4,480 字节。请求次数没有增加。

回归：更新/指标/相关 API 217 项，回测 32 项，Sequoia 16 项，共 265 项通过。新增对照覆盖形态上下文边界、极值平台与缺失值、聚类规则，以及慢请求不阻塞已完成请求。正式服务已加载改动。

## 正式服务观察

2026-09-15 21:46:38 至 21:49:00，今日覆盖从 3,235 只增至 3,683 只，约 143 秒新增 448 只，即 189 只/分钟。此前观察约 63 只/分钟；本段实测约 3 倍，股票样本和网络状态不同，不能承诺每天固定倍数。整体任务未完成时不宣称已补齐。

## 当日数据完整性修复（2026-09-24）

- 接口响应成功不代表目标交易日日线已发布。主源先直接检查目标日指数日线；未发布时由现有容错流程尝试备用源。
- 下载覆盖区间只记到实际收到的数据末日。空返回不登记覆盖，缺失尾部保留给后续重试。
- 每只证券必须有目标交易日的正式日线，或有该日明确的停牌记录，才算同步成功。休市日以交易日历的最近开市日为准；无法确认日历时不能报成功。
- BaoStock 读完结果集后再次检查错误码，防止读取中断被当成正常空返回。
- 已误记的 24 日任务及覆盖元数据已在正式库修正，修改前保存至数据目录 `maintenance/daily-coverage-before-20260924-212828.json`。保留已完成的 346 只，6841 个缺失证券（含 ETF）重新进入补更。此数字是补更启动时的任务状态，不代表补更已完成。
