
## 条件选股需求分析
- 用户需求：近3个月内"任意5日2涨停 或 10日涨幅>40%"。
- 结论：return_N 在每根K线上是滚动N日累计涨幅%，at_least+lookback=60 等价于"3个月内存在某窗口"。10日涨幅>40% 可精确表达；涨停次数无现成指标，用 return_1>9.8 的 at_least(lookback=60, occurrences=2) 近似（不要求集中在5日内）。
- 已写入 screen_burst.json 并通过 validate_tree 校验。
- 引擎局限（备忘）：at_least 只扫最近 lookback 根，无法表达"窗口偏移存在性"；at_least 内部比较硬编码为严格 GT；目录无涨停指标。若要精确语义需新增"近5日涨停次数"指标。

## 服务运行状态检查
- AStock 服务在运行，绑定 Tailscale 地址 100.90.109.126:8888（127.0.0.1 不通，注意用该地址访问）。
- 日线数据停更于 2026-08-19（检查日 09-04，落后约 11 个交易日），无进行中的同步；监控任务列表为空。
- 系统的"实时"是盘中按需拉腾讯快照（live 筛选/监控扫描），K 线库需手动 sync-market 增量更新。

## 8888 服务空白页排查（重要发现）
- 8888 服务实际由 launchd 任务 com.astock-local8888 常驻运行，代码来自 git worktree /Users/lisima/股票api/.worktrees/a-share-screening-engine（分支 feature/a-share-screening-engine），ASTOCK_DATA_DIR 也是该 worktree 的 data/（astock.duckdb 约 1GB）。
- 页面空白原因：浏览器加载的 index.html 引用 index-DpvEVou6.js 返回 404（构建产物与静态资源不一致），React 未挂载。
- 排查期间（00:07-00:15）有自动化任务在 worktree 里活动：前端 dist 00:10 重建、astock.duckdb 00:12 仍在写入，随后服务整体 502（疑似 DuckDB 写锁/重启中）。近期提交："feat: update market data after daily close"。
- 主目录 web/dist（8/24 构建）与运行服务无关。

## Codex 修改同步检查
- Codex 的修改在 worktree worktrees/a-share-screening-engine（feature/a-share-screening-engine 分支），共 2 个提交：c1bf2c8 每日收盘后数据更新（daily_update.py 等 8 文件 +159 行）、537e86c 筛选模板导入保存（10 文件 +474 行）。
- 已核实 ls-remote：两提交均已推送到 GitHub origin/feature/a-share-screening-engine，无需再推。
- 未提交项：worktree 的 .astock-local8888.plist（本机 launchd 部署配置，建议不入库）；主工作区仅 screen_burst.json 和 .workbuddy/（非 Codex 改动）。
- 远程 main 仍是 7d128de，未包含这两个提交。
